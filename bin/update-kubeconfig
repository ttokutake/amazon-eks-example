#!/bin/bash

region=`cat locals.tf | grep region | awk '{print $3}' | sed -e 's/"//g'`
eks_cluster_name=`cat locals.tf | grep eks_cluster_name | awk '{print $3}' | sed -e 's/"//g'`

aws eks update-kubeconfig --region $region --name $eks_cluster_name
